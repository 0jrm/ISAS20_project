Task: build a provenance-traced "project evolution" report and a single self-contained HTML from existing frozen artifacts. Do not run, retrain, or re-score any model. Do not compute a single new headline number.
Read first: HANDOFF.md, PLAN.md, gate_floor_provenance.md, all reports/phase*.md and reports/phase*.json, reports/osse_results.md, reports/data_census.md, reports/split_design.md, NeSPReSO2_onTemplate/evalphys/METRICS_MANIFEST.json, and the PNGs under NeSPReSO2_onTemplate/notebooks/compare_outputs/. Ponytail mode applies: fewest files, stdlib + already-installed deps (matplotlib, numpy, json). No new frameworks, no JS build step, no server.
Step 0 — Build a provenance manifest before anything else. Create reports/evolution/PROVENANCE.json: for every figure and every table you will render, record {artifact_id, source_files[], git_sha, judgment_number, gate_verdict, caveats[]}. If a number you want to show is not present verbatim in a source file, you may not show it — log it under unsourced[] and stop using it. Pull git_sha from METRICS_MANIFEST.json and the current HEAD; if they differ, surface that as a warning banner in the outputs.
Before rendering anything, cross-check the covariance and OSSE artifacts for the corruption/overwrite risk flagged in the agent's own handoff (an osse_results.md was overwritten and restored). If osse_results.md, the covariance summary JSON, and PROVENANCE.json disagree on any E-number, surface the contradiction in a warning banner rather than picking one.

Step 1 — Reconstruct the experiment lineage as data. Produce reports/evolution/lineage.json: a node per experiment (fields: id, phase, hypothesis, change, judgment_number, gate, verdict ∈ {survivor, killed, superseded, erratum}, source_files[]) and edges ({from, to, kind ∈ {led_to, superseded_by, erratum_of}}). Populate strictly from the report headers and the provenance chain in gate_floor_provenance.md. Do not invent hypotheses the reports don't state; if a node's hypothesis is unclear, mark hypothesis: "UNSTATED" rather than guessing.

Step 1b — Datacube documentation strand. Reconstruct the datacube's role from preproc/cube/, preproc/features/, the point_cube/residual_cube configs and pipeline scripts, and the stale-sat diagnostics (h5_stale_check.py, stale_by_split.{md,json}). Produce, in reports/evolution/figs/, generated from artifacts only: (a) an isometric channel-slab cube schematic with a profile pin and highlighted extracted patch (SVG; label lon/lat/time axes and the product channels actually in v2.json/v3.json); (b) a plan-view + time-strip extraction inset showing the real spatial_pad/temporal_pad from the configs; (c) a before/after data-flow diagram (per-station HDF5 patches → regional cube → on-demand interpolation for point/patch/residual); (d) the stale-sat const_frac fingerprint, old vs cube, from the diagnostic outputs. In EVOLUTION.md, state the cube's contribution precisely: data-quality fix, unified extraction geometry, enabling of v3 error channels — and state explicitly that the dissertation winner is a point model, so the cube's payoff is not "patches beat points." Do not imply the patch/residual branch won; if the reports show it losing, say so.

Step 2 — Regenerate figures from artifacts (matplotlib, PNG + SVG):

Lineage DAG — nodes colored by verdict (survivor/killed/superseded/erratum), edges styled by kind. Layout top-to-bottom by phase.
Matrix gate heatmap — 3 representations × 3 heads, cell text = judgment number, glyphs = per-gate PASS/FAIL, winner (A×CRPS) ringed. Include a footnote that PC-space CRPS is not comparable across representations — render those cells with a distinct hatch so no one ranks them.
Depth-band × season heatmaps — CRPS and ENCE, directly from phase5_A_CRPS_physical_strata.md. Mark every cell with ENCE ≥ 0.20 with a visible fail border (the surface, thermocline, and >800 m bands will light up — that is correct, leave it).
OSSE panel — grouped bars E0–E5 overall + by depth band from osse_results.md; annotate E3>E2 FAIL and E4≥E3 FAIL on the figure face, plus the labeled caveats (cast-column proxy, E0≡E1, diagonal-R-only).
Ruler sparkline — floor value over time (0.4158 → 0.514 leaked → 0.5367 clean → 0.5903 floor) from gate_floor_provenance.md.
Reuse existing depth-RMSE / bias / calibration PNGs; do not regenerate them, just reference them with provenance.
Write all figures to reports/evolution/figs/ and add each to PROVENANCE.json.

Step 2b — Structured-covariance strand (canonical result, negative). The make-or-break full-Σ localized R_cal OSSE has been run at protocol strength with pre-registration locked ahead of the read (prereg commit 0422a51, promotion 490de67); verify both commits and the timestamp ordering from git and surface them in the provenance header. Render from the committed osse_results.md / summary JSON only — do not re-run.

E-table + depth-band panel from the canonical 2021 n=1101 run: E2 (ISOP) 0.5410, E3 (NeSPReSO R_fixed) 0.5454, E4 (NeSPReSO full localized Σ_T) 0.6160, E5 (+QC) 1.4008. Render E4≥E3: FAIL and E3>E2: FAIL on the figure face.
The diag-control bar is the headline of this strand — make it the visual center, not a footnote. Show three adjacent bars: E3 R_fixed (0.5454), E4 --rcal diag (0.5463, ≈v1), E4 --rcal full (0.6160). Annotate directly: "localization preserves diag(Σ); the entire 0.546→0.616 degradation is the CRPS-head off-diagonals alone." This single comparison is the scientific result; design the panel so a reader gets it without prose.
R-construction schematic — Σ = V diag(σ²) Vᵀ, rank-16 over 60 levels, ~44-dim near-null space, R = (Σ ∘ ρ_L) + floor·I Schur-tapered to full rank. Include the stabilization evidence (raw full Σ: cond≈2e8, synthetic RMSE≈13.5 → localized: cond≈1.5e4, RMSE≈0.40) framed as "localization is numerically sound; the structure it preserves is the problem" — i.e. the plumbing worked, the science didn't.
In EVOLUTION.md, state the mechanism as the finding, not an aside: the off-diagonal correlations are induced by the shared PCA/EOF basis, not learned; per-dimension CRPS calibration only shaped marginal variances; beneficial cross-level structure would require a learned joint-covariance head this architecture lacks. Do not write "tie" or "inconclusive" — the structured covariance has negative OSSE value here, cleanly isolated.

Step 3 — Write reports/evolution/EVOLUTION.md. Structure: (1) one-paragraph honest abstract; (2) timeline of phases as a table (phase, question, change, verdict, source); (3) the lineage narrative walking the DAG including the dead branches and the ruler repair as first-class events; (4) the matrix result stated plainly — B won deterministic RMSE (0.534), A won the probabilistic crown (A×CRPS), C (density/spice) lost both; (5) the calibration reality — clears ENCE in PC space, fails in physical space at 0.236, worst in the thermocline; (6) the OSSE result stated without spin — NeSPReSO ties ISOP (0.545 vs 0.541), calibrated R ties diagonal; (7) a "what is genuinely established vs. what is not" ledger. Every quantitative claim gets an inline source path. No number appears that isn't in PROVENANCE.json. Do not use words like "success," "strong," or "improvement" for a null/failed gate.  The "genuinely established vs not" ledger must record: the calibrated-ML-pseudo-obs-improve-DA thesis is empirically falsified in the cast-column GoM testbed — NeSPReSO ties ISOP on fixed R and its structured covariance actively degrades the analysis, with the cause isolated to basis-induced off-diagonals via the diag control. Scope the claim honestly (cast-column proxy, single region, no map-level ISAS truth / horizontal localization). Frame this as a closed question and a clean negative, and cross-reference it to the reframing the ledger should now recommend: the dissertation's defensible contribution is the evaluation-standard-plus-methodology-plus-demonstrated-negatives package, and the thesis objective should be formally retired in favor of it. Do not let the quality of the negative result be rendered as if the thesis partially worked.

Step 4 — Build reports/evolution/index.html, single self-contained file. Embed lineage.json and the summary tables as inline JSON; inline the SVGs; no external network calls, no server, no localStorage. Sections/tabs: Overview · Lineage (interactive DAG — click a node to see hypothesis/change/verdict/source) · Matrix · Calibration strata · OSSE · Provenance (render PROVENANCE.json as a table so every figure's source and git_sha is visible). Gate verdicts render in true colors — FAIL is red even when it's the flagship model. Put a persistent header line: git_sha=…, evalphys=…, generated=…, artifacts frozen — no re-scoring.
Step 5 — One runnable check (ponytail rule). Add reports/evolution/check_provenance.py: asserts every figure and table referenced in EVOLUTION.md and index.html exists in PROVENANCE.json, that no unsourced[] entries leaked into the outputs, and that every gate verdict in the HTML matches the source JSON. It must exit non-zero if any number in the HTML lacks a source.
Constraints: touch nothing under saved/ or data/; create only under reports/evolution/; if any source file is missing or self-contradictory (e.g. the stale_by_split MD says gate OPEN while the JSON says embargoed:false), surface the contradiction in the report rather than silently picking one.
